# Vivaldi flags — read by /usr/bin/vivaldi-stable on startup
# Native Wayland + PipeWire screen sharing for Google Meet and similar
--ozone-platform=wayland
--enable-features=WebRTCPipeWireCapturer
--enable-features=VaapiVideoDecodeLinuxGL
