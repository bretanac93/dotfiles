#!/bin/zsh
# Example local aliases file
# Copy this to ~/.config/zsh.local/alias/local.zsh and customize

# Work-specific aliases
# alias work-project='cd ~/Work/project-name'
# alias work-logs='tail -f /var/log/work/app.log'

# AWS shortcuts
# alias aws-prod='aws --profile production'
# alias aws-dev='aws --profile development'

# Docker shortcuts
# alias d='docker'
# alias dc='docker-compose'
# alias k='kubectl'

# Navigation
# alias proj='cd ~/Code/projects'
# alias dots='cd ~/Code/dotfiles'

# Custom git aliases (in addition to the shared ones)
# alias gwip='git add -A && git commit -m "WIP"'
# alias gundo='git reset --soft HEAD~1'
