# Example local login-shell overrides
# Copy this to one of these files and customize:
#   ~/.config/zsh.local/profile.zsh
#   ~/.config/zsh.local/profile.<short-hostname>.zsh  (hostname from ${HOST%%.*})

# Prefer Colima Docker on this machine.
# if [[ -S "$HOME/.colima/default/docker.sock" ]]; then
#   export DOCKER_HOST="unix://${HOME}/.colima/default/docker.sock"
# fi

# Use a machine-specific JAVA_HOME or other login-shell overrides.
# export JAVA_HOME="/Library/Java/JavaVirtualMachines/temurin-21.jdk/Contents/Home"
